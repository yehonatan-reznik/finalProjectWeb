# Test Assets

`images/` contains local still images for quick detection checks.

Use these assets to verify:

- the browser can load the image
- the detector finds the expected object class
- center-point math and overlay drawing look correct

These files are only for local validation and demo support.
