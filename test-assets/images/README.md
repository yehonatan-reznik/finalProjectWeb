# Test Images

This folder contains still images used for quick detector checks.

Use them to confirm that:

- the browser can open the file cleanly
- the detector labels the expected object
- the overlay box and center-point math look correct
