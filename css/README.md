# CSS

This folder contains the shared site styling.

- `style.css` defines the dashboard theme, page layout, cards, forms, buttons, and responsive behavior used across all HTML pages.

Keep reusable visual rules here instead of scattering page-specific inline styles across the HTML files.
