# Models

This folder stores machine-learning model files used by the browser detector.

- `aeroyolo.onnx` is the real AeroYOLO ONNX model loaded by `js/pages/control-detection.js`.

This file is binary model data, not readable text. If it looks corrupted in an editor, that is expected.
