# Docs

`source-material/` contains local project documents that were previously stored at the workspace root.

These files are reference material only and are not used directly by the web app or firmware.

Keep engineering notes, test procedures, and future calibration records here instead of mixing them into the web app folders.

Included now:

- `testing-checklist.md` for camera, detection, and bench-test verification
- `source-material/` for original project documents
